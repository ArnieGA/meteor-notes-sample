import { Meteor } from 'meteor/meteor';
import React from 'react';
import expect from 'expect';
import { mount } from 'enzyme';


if(Meteor.isClient){
    describe('EmailVerification Component Tests', function(){
        it('These tests are still pending');
    });
}